name: Build dev-latest

on:
  workflow_dispatch:
  push:
    branches: [master]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    uses: ./.github/workflows/build-and-push.yml
    with:
      tags: dev-latest
    secrets: inherit
